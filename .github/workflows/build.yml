name: Build and Push Portfolio

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log into GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build image
        run: |
          docker build \
            -t ghcr.io/lt-dogfish/portfoliosite:latest \
            -t ghcr.io/lt-dogfish/portfoliosite:${{ github.sha }} \
            .

      - name: Push image
        run: |
          docker push ghcr.io/lt-dogfish/portfoliosite:latest
          docker push ghcr.io/lt-dogfish/portfoliosite:${{ github.sha }}
